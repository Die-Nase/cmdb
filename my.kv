#: import TreeViewLabel kivy.uix.treeview.TreeViewLabel

<FloatLayout>:
    name: "test"
    BoxLayout:

        TreeView:
            id: tv
        Button:
            size: (200,50)
            size_hint: (None, None)
            text: "add_node"
            on_press: tv.add_node(TreeViewLabel(text = "Node..."))