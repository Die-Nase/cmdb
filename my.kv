#: import TreeViewLabel kivy.uix.treeview.TreeViewLabel
#:import GetSystemMetrics win32api.GetSystemMetrics


<DatabaseManager>:
    #size: GetSystemMetrics(0), GetSystemMetrics(1)
    #size: 1000, 400

    BoxLayout:
        size_hint: .5, 1
        pos_hint: {"x":0, "top":1}

        TreeView:
            id: tv
            hide_root: True

        Button:
            size: (200,50)
            size_hint: (None, None)
            text: "add_node"
            on_press: root.say_hello()
            